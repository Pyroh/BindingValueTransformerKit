    import XCTest
    @testable import BindingValueTransformerKit

    final class BindingValueTransformerKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(BindingValueTransformerKit().text, "Hello, World!")
        }
    }
